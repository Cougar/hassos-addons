---
configuration:
  verbose:
    name: Log level
    description: Number for the log level verbosity (default 0)
  cmdline_extra_args:
    name: Command line extra arguments
    description: Extra arguments to pass to the "cadvisor" command at startup (see https://github.com/google/cadvisor/blob/master/README.md for more information)
